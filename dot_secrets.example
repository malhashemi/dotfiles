#!/usr/bin/env bash
# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║  Secret Environment Variables - EXAMPLE                                       ║
# ║  Copy to ~/.secrets and fill in your values                                   ║
# ╚══════════════════════════════════════════════════════════════════════════════╝
#
# For chezmoi users with Bitwarden (RECOMMENDED):
#   1. Create a Bitwarden item named "dotfiles-secrets"
#   2. Add custom fields for each secret below (use "Hidden field" type for sensitive values)
#   3. Run: bw login
#   4. Run: bw unlock --raw > ~/.bitwarden_session && chmod 600 ~/.bitwarden_session
#   5. Run: chezmoi apply
#
# The session persists in ~/.bitwarden_session - no need to unlock again until
# you change your master password or run 'bw logout'
#
# For manual setup (without Bitwarden):
#   1. Copy this file to ~/.secrets
#   2. Fill in your values
#   3. Run: chmod 600 ~/.secrets

# ─────────────────────────────────────────────────────────────────────────────────
# iDrive e2 S3-Compatible Storage
# Get these from: https://www.idrive.com/e2/account -> Access Keys tab
# ─────────────────────────────────────────────────────────────────────────────────
export IDRIVE_ACCESS_KEY_ID="your-access-key-here"
export IDRIVE_SECRET_ACCESS_KEY="your-secret-key-here"
export IDRIVE_ENDPOINT="your-endpoint.idrivee2-XX.com"
export IDRIVE_REGION="your-region"

# ─────────────────────────────────────────────────────────────────────────────────
# AI/LLM API Keys
# ─────────────────────────────────────────────────────────────────────────────────
export BRAVE_API_KEY="your-brave-api-key"
export CLAUDE_CODE_OAUTH_TOKEN="your-claude-oauth-token"
export EXA_API_KEY="your-exa-api-key"

# ─────────────────────────────────────────────────────────────────────────────────
# Adding New Secrets
# ─────────────────────────────────────────────────────────────────────────────────
# All custom fields are automatically exported - no template changes needed!
#
# To add a new secret:
#   1. Add a custom field to the "dotfiles-secrets" item in Bitwarden
#   2. Run: bw sync && chezmoi apply
#
# Example fields you might add:
#   GITHUB_TOKEN, OPENAI_API_KEY, ANTHROPIC_API_KEY, AWS_ACCESS_KEY_ID, etc.
