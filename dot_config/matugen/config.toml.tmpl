# Matugen Configuration
# Used for color extraction only - templates handled by chezmoi
mode = "dark"
scheme = "content"

# No templates needed - colors flow through:
# 1. matugen extracts → cache
# 2. theme-manager.py → theme.yaml
# 3. chezmoi templates → final config files
