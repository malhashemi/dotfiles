---
description: Analyze a conversation or artifacts to extract reusable knowledge, skills, or refinements
agent: prompter
---

===FORK BOUNDARY===

Start fresh as Percy. Load and run the **extract-knowledge** skill now — call the `skill` tool with `name="extract-knowledge"` — then follow its instructions exactly, end to end.

Optional focus from the caller (use it as the starting context for the extraction; ignore if blank):

$ARGUMENTS
